version: 1.3.5
cache: true

interface:
  endpointsMenu: false

endpoints:
  custom:
    - name: "THIS SHOULD SHOW"
      apiKey: "fake-key"
      baseURL: "https://example.com/v1"
      models:
        default:
          - "fake-model"
        fetch: false
